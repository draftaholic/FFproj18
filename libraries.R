install.packages(
  c(
    "dplyr",
    "ggridges",
    "ggplot2",
    "jsonlite",
    "gsheet",
    "readr"
  )
)

library(dplyr)
library(ggridges)
library(ggplot2)
library(jsonlite)
library(gsheet)
library(readr)
